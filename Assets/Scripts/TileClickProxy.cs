using UnityEngine;

namespace NexusGame
{
    // Lightweight component to map a collider hit back to a board tile via its axial coordinates.
    public class TileClickProxy : MonoBehaviour
    {
        public int Q;
        public int R;
    }
}

